name: Formatting

on:
  push:
    branches:
      - '**'

  pull_request:
    branches: [master]

jobs:
  Prettier:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Check Formatting
        run: |
          npm install 
          npm run format
